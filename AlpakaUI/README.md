# Psy&Pro UI

Веб-платформа для психологической диагностики и профессиональной ориентации с интеграцией внешней системы обработки данных.

## 📋 Описание

Psy&Pro UI - это современная веб-платформа, предназначенная для проведения психологических диагностик и профориентационных тестирований. Система обеспечивает:

- **Два режима тестирования:**
  - Психологическая диагностика (PSYCHODIAGNOSTICS)
  - Профессиональная ориентация (CAREER_GUIDANCE)

- **Автоматическую обработку данных:**
  Ответы пользователей автоматически отправляются во внешнюю агентскую систему для профессиональной обработки и генерации персональных отчетов

- **Удобный пользовательский интерфейс:**
  - Адаптивный дизайн для всех устройств
  - Dark/Light режимы
  - Интерактивный чат-интерфейс для прохождения тестов

## 🏗️ Архитектура системы

```
┌─────────────────────┐
│   Psy&Pro UI        │
│   (Frontend)        │
│                     │
│  • Next.js 15       │
│  • React 19         │
│  • TypeScript       │
│  • Prisma ORM       │
└──────────┬──────────┘
           │
           │ REST API
           │
┌──────────▼──────────┐
│   Neon PostgreSQL   │
│   (База данных)     │
└──────────┬──────────┘
           │
           │ Jobs Queue
           │
┌──────────▼────────────┐
│  Внешняя агентская    │
│  система обработки    │
│  данных               │
│                       │
│  • Polling API        │
│  • Генерация отчетов  │
│  • Обработка данных   │
└───────────────────────┘
```

### Интеграция с внешней системой

Платформа использует асинхронную архитектуру для обработки данных:

1. Пользователь заполняет анкету
2. Данные сохраняются в БД и помещаются в очередь (`queued`)
3. Внешняя система опрашивает API каждые 10 секунд (`GET /api/external/jobs?status=queued`)
4. Система забирает задачу и обрабатывает (`processing`)
5. Готовые отчеты отправляются обратно (`PATCH /api/external/jobs/[id]`)
6. Пользователь получает уведомление о готовности отчетов

## 🚀 Технологический стек

### Frontend
- **Next.js 15.5.2** - React фреймворк с App Router
- **React 19** - UI библиотека
- **TypeScript 5.8** - Типизация
- **Tailwind CSS 4** - Стилизация
- **Framer Motion** - Анимации
- **Zustand** - State management

### Backend & Database
- **Prisma ORM 6.16.3** - Database ORM
- **PostgreSQL 14+** - Реляционная база данных
- **bcryptjs** - Хеширование паролей
- **JWT** - Аутентификация

### Дополнительно
- **Resend** - Email-сервис для отправки отчетов
- **Winston** - Structured logging
- **Zod** - Runtime validation

## 📦 Установка и запуск

### Требования
- Node.js 18+ (рекомендуется 20+)
- npm или yarn
- PostgreSQL 14+ база данных

### Установка зависимостей

```bash
npm install
```

### Настройка окружения

Создайте файл `.env.local`:

```env
# Database
DATABASE_URL="postgresql://user:password@host/database"

# JWT Authentication
JWT_SECRET="your-jwt-secret-key"
JWT_REFRESH_SECRET="your-refresh-secret"
NEXTAUTH_SECRET="your-nextauth-secret"

# External Integration
BACKEND_SECRET="your-integration-secret"
BACKEND_PROCESSING_URL="https://external-system-url"

# Email Service
RESEND_API_KEY="your-resend-api-key"

# Optional
AI_SERVICE_KEY="your-ai-service-key"
```

### Инициализация базы данных

```bash
# Применить миграции
npx prisma migrate deploy

# Генерация Prisma Client
npx prisma generate

# (Опционально) Seed данных
npx prisma db seed
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:3000`

### Сборка для production

```bash
npm run build
npm run start
```

## 📁 Структура проекта

```
psypro-ui/
├── prisma/               # Database schema и миграции
│   └── schema.prisma
├── src/
│   ├── app/             # Next.js App Router
│   │   ├── api/        # API Routes
│   │   ├── admin/      # Админ панель
│   │   ├── auth/       # Страницы аутентификации
│   │   └── page.tsx    # Главная страница
│   ├── components/      # React компоненты
│   │   ├── admin/      # Админ компоненты
│   │   └── ui/         # UI компоненты
│   ├── lib/            # Утилиты и конфигурация
│   │   ├── auth/       # JWT, Password hashing
│   │   ├── email/      # Email интеграция
│   │   ├── errors/     # Error handling
│   │   └── prisma.ts   # Prisma client
│   ├── services/       # Бизнес-логика
│   ├── store/          # Zustand stores
│   ├── hooks/          # React hooks
│   ├── utils/          # Вспомогательные функции
│   └── middleware.ts   # Next.js middleware
├── public/             # Статические файлы
└── package.json
```

## 🔐 Безопасность

### Аутентификация и авторизация
- JWT tokens с refresh mechanism
- Роли пользователей: ADMIN, CONSULTANT, SUPPORT, USER
- Session management с автоматическим logout
- Rate limiting на API endpoints

### Защита данных
- Bcrypt для хеширования паролей (rounds: 10)
- HTTPS обязателен для production
- CORS настроен для внешней интеграции
- Environment variables для всех секретов

### API Security
- Валидация входных данных через Zod
- Middleware для проверки JWT
- Rate limiting (Auth: 5 req/15min, API: 100 req/min)
- Audit logging всех критичных операций

## 👥 Роли пользователей

| Роль | Описание | Доступ |
|------|----------|--------|
| **ADMIN** | Администратор системы | Полный доступ ко всем функциям |
| **CONSULTANT** | Психолог/консультант | Управление клиентами и просмотр отчетов |
| **SUPPORT** | Техподдержка | Помощь пользователям, ограниченный доступ |
| **USER** | Обычный пользователь | Прохождение тестов, просмотр своих отчетов |

## 📊 Статусы сессий

- `IN_PROGRESS` - Пользователь проходит тест
- `COMPLETED` - Тест завершен, отправлен на обработку
- `QUEUED` - Ожидает обработки внешней системой
- `PROCESSING` - Обрабатывается внешней системой
- `FAILED` - Ошибка обработки

## 🔌 API Integration

### Внешняя система (Polling)

**GET** `/api/external/jobs?status=queued`
```typescript
// Request headers
{
  "X-Backend-Secret": "your-secret-key"
}

// Response
{
  "jobs": [{
    "jobId": "uuid",
    "sessionId": "uuid",
    "mode": "PSYCHODIAGNOSTICS" | "CAREER_GUIDANCE",
    "responses": {
      "question_0": "user answer...",
      "question_1": "user answer..."
    },
    "userData": {
      "firstName": "string",
      "lastName": "string",
      "email": "string",
      "phone": "string"
    }
  }]
}
```

**PATCH** `/api/external/jobs/[id]`
```typescript
// Request
{
  "status": "completed" | "failed",
  "reports": {
    "Professional Report": "content...",
    "Adapted Report": "content...",
    "Aggregate Score Profile": "content..."
  },
  "error": "error message if failed"
}
```

## 📧 Email Integration

Система использует Resend для отправки email с готовыми отчетами:

- Автоматическая отправка при готовности отчетов
- PDF вложения с отчетами
- Профессиональный HTML-шаблон
- Поддержка кириллицы (Roboto font)

## 🚀 Развертывание

### Production сервер

```bash
# Установить зависимости
npm install --production

# Применить миграции БД
npx prisma migrate deploy

# Собрать production build
npm run build

# Запустить
npm run start
```

### Environment Variables

Создать файл `.env.production` на сервере с переменными окружения

### Database

Использовать PostgreSQL 14+ (локальный или удаленный сервер)

Подробная инструкция по установке в документе **"Инструкция по установке и настройке.pdf"**

## 📝 Лицензия

Проприетарное программное обеспечение. Все права защищены.


**Год разработки:** 2025

## 👨‍💻 Разработка

Для разработки используется:
- ESLint для линтинга
- Prettier для форматирования (опционально)
- TypeScript strict mode
- Git flow для версионирования

## 🐛 Логирование

Система использует Winston для structured logging:

```typescript
logger.info('message', { metadata });
logger.error('error message', error);
logger.warn('warning', { context });
```

Логи сохраняются в файлы `logs/` и выводятся в console.

---

**Версия:** 1.0.0
**Дата релиза:** Ноябрь 2025
**Node.js:** 18+
**Next.js:** 15.5.2
